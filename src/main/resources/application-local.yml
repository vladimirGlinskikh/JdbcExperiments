spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://172.31.0.1:3307/bookdb2?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    hibernate:
      ddl-auto: validate
  sql:
    init:
      mode: always
  flyway:
    user: root
    password: root
#  liquibase:
#    user: root
#    password: root